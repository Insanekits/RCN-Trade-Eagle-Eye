{
  "name": "rcn-trade-eagle-eye",
  "version": "1.0.0",
  "private": true,
  "description": "Static Trade Eagle Eye dashboard for RCN trade monitoring with Excel-to-JSON sync support.",
  "scripts": {
    "dev": "python -m http.server 5173",
    "sync": "python scripts/sync_excel.py --env sync.env",
    "validate": "python scripts/sync_excel.py --env sync.env --validate-only"
  },
  "keywords": [
    "rcn",
    "trade",
    "dashboard",
    "excel-sync"
  ],
  "license": "UNLICENSED"
}
